# RAG 零基础面试讲解（配合本项目真实代码）

> **这份文档写给谁**：完全没学过 RAG、不写 Python 的你。目标只有一个——面试时能脱稿讲清「RAG 是什么、为什么用它、怎么运转、我自己项目里是怎么实现的」。
> 文中所有代码都摘自本项目的 `agent-server/` 目录，面试官要看代码可以逐段指认。
> 配套：《AI心理健康项目三大亮点.md》《AI心理健康项目RAG升级代码对照.md》
>
> **阅读顺序**：第〇节先背 → 第一节建立"为什么" → 第二节补概念（看懂 RAG 的全部前置知识就这 5 个）→ 第三节串链路 → 第四节对着自己的代码过一遍 → 第六、七节考前复习。

---

## 〇、先背这两个版本（面试开场直接用）

### 30 秒电梯版

> RAG 就是让 AI 回答问题之前先查资料。把用户的提问变成向量，到预先建好的"向量知识库"里找出最相关的几段原文，拼进提示词再让大模型生成回答——相当于**开卷考试**：模型不用背下所有知识，但要会查、会用。我的项目里，知识库是 30 篇心理健康文章切成的 122 个知识块，用 ChromaDB 做余弦相似度检索取 top-3，检索和生成都在 Python 服务端完成，前端只负责接收流式结果和渲染引用卡片。

### 60 秒标准版

> RAG 分两条流水线。**离线流程**负责建资料库：加载 30 篇文章 → 按小节标题切成 122 块 → 每块调 embedding 模型转成 1024 维向量 → 连同原文一起存进向量数据库。**在线流程**负责回答问题：用户提问 → 用同一个 embedding 模型把问题也变成向量 → 在库里做余弦相似度匹配取最相关的 3 块 → 把这 3 块原文和人设提示词拼在一起发给大模型 → 流式生成回答，同时把命中的文章信息作为"引用卡片"推给前端。这样模型回答有依据、能标出处，还解决了大模型不知道私有数据、爱编造的问题。工程上我做了语料指纹增量重建、3 秒软超时降级、双向量库后端兜底这些可靠性设计。

---

## 一、为什么需要 RAG：大模型的三个天生短板

大语言模型（下称 LLM，比如 ChatGPT、智谱 GLM）很聪明，但有三个天生的问题，RAG 就是冲着它们来的：

| 短板 | 白话解释 | 在我项目里的体现 |
|---|---|---|
| **知识有截止日期** | 模型的知识来自训练时"读"过的数据，训练完之后发生的事它不知道 | 模型没读过我们平台上传的这 30 篇心理文章 |
| **不认识你的私有数据** | 你自己的文档、数据库、内部知识，模型从来没见过 | 课程要求的文章库是平台自己的内容 |
| **幻觉（Hallucination）** | 不知道也硬编，一本正经胡说八道 | 心理咨询场景**绝对不能**出现编造的建议 |

### 类比：开卷考试

- **不开卷的 LLM** = 一个只带脑子进考场的学霸：会的题答得很好，没学过的题就开始编。
- **RAG** = 开卷考试：考前先把相关资料页翻出来（检索），贴在试卷旁边（拼进提示词），让他照着资料答题（生成）。他不需要背下整本书，只需要**会找、会读、会答**。

### 为什么不选另外两条路？

| 方案 | 做法 | 为什么没选 |
|---|---|---|
| 全文塞进提示词 | 每次提问把 30 篇文章全贴给模型 | 文章一多就超长度上限，又慢又贵，检索才是精准投喂 |
| 微调（Fine-tuning） | 拿资料重新训练模型 | 成本高、周期长、更新一次资料就要重训；而且心理文章更新频繁，不划算 |
| **RAG** ✅ | 资料变化只需重建向量库，分钟级；回答能附带出处 | —— |

**面试一句话**：资料频繁更新、要出处、要低成本 → RAG；要改变模型的风格/能力 → 才考虑微调。两者还能组合。

---

## 二、看懂 RAG 的 5 个前置概念

> 这 5 个概念搞明白，任何 RAG 文章/架构图你都能看懂。每个概念都按「是什么 → 类比 → 在我项目里对应什么」讲。

### 概念 1：大语言模型（LLM）——一台超级"接话"机器

**是什么**：LLM 本质上在做一件事——给定前面所有文字，预测下一个字（词元 token）是什么。"你好，"后面接"吗"的概率高，接"香蕉"的概率低。把这件事在万亿级文本上练到极致，就涌现出了对话、写代码等能力。

**对我们的意义**：它没有"数据库"，没有"记忆"，只有训练时压缩进去的语言规律。所以它不知道你的私有文章（没读过），也不知道今天几号（训练截止了）。

**项目对应**：用智谱 `glm-4-flash-250414`（固定版本号而不是别名，是为了首字延迟稳定，实测别名会抖到 1.8s，固定版本 190~240ms）。

### 概念 2：Embedding（向量化）——给每段文字发一张"语义坐标"

**是什么**：计算机不懂文字，只懂数字。Embedding 模型把一段文字翻译成**一串数字**（向量），比如 1024 个数字。这 1024 个数字可以想象成一张 1024 维"语义地图"上的一个坐标点。

**关键性质**：**意思相近的文字，坐标就相近**，哪怕用词完全不同。

- "我最近总是失眠" 和 "晚上睡不着怎么办" → 字面几乎不重叠，坐标却挨得很近
- "我最近总是失眠" 和 "今天吃了火锅" → 坐标离得很远

这就是"语义检索"的根基：匹配的是**意思**，不是**字面**。

**项目对应**：智谱 `embedding-2` 模型，输出 1024 维向量。文章的每一块、用户的每个提问，都要先过这道"翻译"手续。

### 概念 3：余弦相似度（Cosine Similarity）——用"方向夹角"衡量两段话多像

**是什么**：有了两个坐标（向量），怎么量化"有多近"？最常用余弦相似度：把两个向量想象成从原点出发的两支箭，看**夹角**——

- 方向完全相同 → 相似度 = 1（意思一样）
- 垂直 90° → 相似度 = 0（没关系）
- 方向相反 → 相似度 = -1（意思相反）

**为什么看角度不看长度**：长文章和短问题只要主题一致、方向就一致。一段 500 字讲失眠的文章，和一个 8 个字的提问"最近失眠怎么办"，用余弦照样能匹配上。这就是为什么检索前不比长度。

**项目对应**：向量数据库 ChromaDB 配置的就是 cosine 距离空间。它返回的"距离"= 1 − 相似度，代码里换算回统一的分数：`score = 1 - distance`（越大越相似，1 封顶）。实测"失眠"类提问命中最高的文章块 score 约 0.64。

### 概念 4：向量数据库——按"意思"查找的仓库

**是什么**：专门存向量、并支持"给我一个向量，秒回最相似的 K 个"的数据库。

**为什么 MySQL 不行**：传统查询是字面匹配——`WHERE 标题 LIKE '%失眠%'`，用户问"晚上睡不着"就啥也搜不到（标题里没有"睡不着"三个字）。向量数据库比的是坐标距离，即**语义**。

**内部原理一句话**：库里的向量建了"近似最近邻（ANN）"索引（Chroma 用的是 HNSW 图索引），不用逐一比对全部向量，牺牲一丁点精度换几百倍速度。我只有 122 块，就算全量暴力比对也是毫秒级——这是选型的底气。

**常见产品**：FAISS（Meta 的开源库）、Pinecone（云服务）、Milvus（国产开源）、**ChromaDB（本项目用的）**。

**为什么选 ChromaDB**（面试常问）：**嵌入式**——像装一个普通代码包一样装进服务里就能用，不需要单独部署一台数据库服务器（免费部署平台 512MB 内存也扛得住）；自带落盘（PersistentClient），重启不用重新花 embedding 钱；百级数据量下性能与 FAISS/Pinecone 无差别。

### 概念 5：文本分块（Chunking）——把整本书剪成知识卡片

**是什么**：入库前把长文章切成较小的块（chunk），每块独立向量化、独立被检索。

**为什么必须切**：
1. embedding 模型有输入长度上限，整篇超限；
2. 检索粒度：用户问"失眠怎么办"，需要的是失眠那一小节，不是整篇 3000 字文章（其中大半在讲别的）；
3. 最终检索结果要拼进提示词，块太大 → 提示词又长又贵又稀释重点。

**切太大 / 切太碎的病**：
- 太大 → 检索不精准、提示词臃肿（检索回来 3 块等于塞了大半篇原文）；
- 太碎 → 上下文丢失（"以下三种方法："单独成块，谁都不知道指什么）。

**怎么切才好**：按**天然语义边界**切，而不是无脑按字数切。本项目按文章的 `<h3>` 小节标题切——每块自带小节标题，语义完整。30 篇 → 122 块。

---

## 三、RAG 完整链路：两条流水线

### 全景图（文字版）

```
【离线流程：建资料库】（项目里 = 服务启动时自动执行）

  30篇文章(JSON)          每篇按<h3>小节切块         122块逐批调embedding接口          向量数据库
  data/articles-*.json ──▶ 碎块<20字丢弃 ──▶ 10块/批转成1024维向量 ──▶ ChromaDB(cosine) 入库索引
                            共得122块                 连原文+标题+小节一起存

【在线流程：回答问题】（项目里 = 用户每次发消息时）

  用户提问           同一个embedding模型        ChromaDB余弦比对122块      3块原文拼进system prompt
  "最近总是失眠" ──▶ 也转成1024维向量 ──▶ 取最相似的top-3块 ──▶ + 人设提示词一起发给GLM
                                                                        │
  前端打字机渲染 ◀── SSE流式(token一个个吐) + 引用卡片数据(citations首事件) ◀─┘
```

**两条线的衔接点**：离线流程把向量存进库；在线流程拿用户的向量去库里找。两条线用的是**同一个 embedding 模型**——必须同款，否则两张"语义地图"坐标系不同，比距离没有意义（面试爱考这个细节）。

### 离线流程 4 步（每步干什么、为什么）

| 步骤 | 干什么 | 为什么必须有 |
|---|---|---|
| ① 加载文档 | 读取 30 篇文章 JSON | 知识的源头 |
| ② 文本切分 | 按 `<h3>` 小节切，剥掉 HTML 标签，<20 字碎块丢弃 | 概念5：检索粒度 |
| ③ 向量化 | 每块加上"【分类】标题 - 小节"前缀后调 embedding-2，10 块/批 | 概念2：文字→坐标；前缀给向量补充主题上下文，提升命中率 |
| ④ 入库索引 | 向量 + 原文 + 标题/小节元数据一起写入 Chroma | 概念4：之后才能按语义秒查 |

### 在线流程 4 步（每步干什么、为什么）

| 步骤 | 干什么 | 为什么必须有 |
|---|---|---|
| ① Query 向量化 | 把用户这句话也变成 1024 维向量 | 和库里的块处于同一坐标系才能比 |
| ② 向量匹配 | 余弦相似度比对全部 122 块，取 top-3 | 把"最相关的资料"从库里捞出来 |
| ③ Prompt 组装 | 3 块原文（各截前 300 字）+ 人设提示词拼成 system prompt | 让模型"开卷"——照着资料答 |
| ④ LLM 生成 | 发给 GLM-4-flash 流式生成，SSE 逐 token 推给前端 | 生成最终回答 |

### 用一次真实提问走完全程

用户在咨询页输入：**"最近总是失眠怎么办"**

1. 前端把 `{message: "最近总是失眠怎么办", history: [最近10条历史]}` POST 到 `/agent/rag/chat`（经 Netlify 边缘函数转发到 Render 上的 Python 服务）。
2. 服务端把这句话变成 1024 维向量。
3. ChromaDB 拿这个向量和 122 块逐一比余弦，返回最相似的 3 块——实测会命中《失眠的认知行为疗法(CBT-I)》等文章的相应小节，最高分约 0.64。
4. 先推一个 `citations` 事件给前端（文章标题、小节、相似度）——**此时回答一个字还没生成，引用卡片已经先渲染出来了**。
5. 把 3 块原文 + "温暖的心理陪伴助手"人设提示词拼成 system prompt，连同历史消息一起发给 GLM。
6. GLM 一边生成一边把 token 用 SSE 推回来，前端 rAF 打字机逐字渲染。
7. 结束推 `done` 事件；流式结束后前端再异步跑一轮情绪分析（`/agent/analyze`）刷新情绪花园。

---

## 四、本项目的实现（真实代码 + 白话解读）

### 4.0 技术选型一览

| 环节 | 选型 | 一句话理由 |
|---|---|---|
| LLM | 智谱 glm-4-flash-250414（固定版本） | 免费额度、首字快且稳定（190~240ms） |
| Embedding | 智谱 embedding-2，1024 维 | 中文效果好、免本地跑模型（512MB 内存跑不动本地模型） |
| 向量库 | ChromaDB（嵌入式） | 免独立服务、自带落盘；另备纯 Python 余弦的 JsonStore 兜底 |
| 服务框架 | FastAPI（Python） | 原生异步，天生适合 SSE 流式 |
| 部署 | Render（免费层）+ Netlify 边缘函数转发 | 全免费；密钥只在 Render 环境变量里 |

### 4.1 离线流程的真实代码（agent-server/knowledge_base.py）

**第②步：文本切分**（把整篇 HTML 文章切成带标题的知识块）：

```python
def _chunk_article(article: dict) -> list[dict]:
    html = article.get("content") or ""
    title = article.get("title") or ""
    category = article.get("categoryName") or ""
    chunks = []
    # (?=<h3>) 是"前瞻"写法：在每个<h3>小节标题前下刀，但保留标题本身
    for sec in filter(None, (s.strip() for s in re.split(r"(?=<h3>)", html))):
        m = re.search(r"<h3>(.*?)</h3>", sec)
        heading = m.group(1) if m else title        # 这一块的小节标题
        text = re.sub(r"<[^>]+>", "", sec).strip()  # 剥掉HTML标签，留纯文字
        if len(text) < 20:                          # 不足20字的碎块没有检索价值
            continue
        chunks.append({
            "id": f"{article['id']}_{len(chunks)}",   # 稳定id：重启后不漂移
            "articleId": article["id"],
            "articleTitle": title,
            "heading": heading,
            "text": text,
            # 真正送去向量化的文本带上前缀，给向量补充主题信息
            "embed_text": f"【{category}】{title} - {heading}\n{text}",
        })
    return chunks
```

> 白话：对每篇文章，遇到小节标题就剪一刀，每块记下"属于哪篇、小节叫什么、正文是什么"，再额外准备一份"带标题前缀的版本"拿去做向量化。30 篇文章这样剪出 122 块。

**第③④步：批量向量化 + 写入**（`_ingest` 函数核心）：

```python
async def _ingest(store) -> int:
    fp = _seed_fingerprint()                  # 全部语料文件的sha256指纹
    store.clear()                             # 清掉旧库
    chunks = []
    for article in _load_seed_articles():
        chunks.extend(_chunk_article(article))    # 全量切块
    async with httpx.AsyncClient(timeout=60) as client:
        # 122块按10块/批调embedding接口，拿到122个1024维向量
        vectors = await embed_texts([c["embed_text"] for c in chunks], client)
    valid = [(c, v) for c, v in zip(chunks, vectors) if v]
    store.upsert(                              # 向量+原文+元数据一起入库
        ids=[c["id"] for c, _ in valid],
        vectors=[v for _, v in valid],
        metadatas=[{"articleId": c["articleId"],
                    "articleTitle": c["articleTitle"],
                    "heading": c["heading"]} for c, _ in valid],
        documents=[c["text"] for c, _ in valid],
    )
    store.set_fingerprint(fp)                  # 记下"这批语料建出来的库"
    return len(valid)
```

> 白话：切块 → 分批"翻译"成向量 → 连人带档案（向量、原文、标题）一起存进 ChromaDB → 最后在库上盖一个"语料指纹"章。下次启动时先对指纹：语料没变就直接用旧库（省一遍 embedding 调用），变了才重建。

**入库的触发时机（工程设计，面试加分）**：不是手动跑脚本，而是挂在服务启动的 lifespan 里后台自动执行；Render 免费层的磁盘是"易失"的（重启/休眠唤醒数据就没了），所以**每次冷启动都靠指纹校验自动恢复知识库**，全程无人工介入。实测 30 篇重建约 4.8s。

### 4.2 在线流程的真实代码

**第①②步：问题向量化 + 检索**（agent-server/rag.py）：

```python
async def retrieve(query: str, top_k: int | None = None) -> list[dict]:
    query = (query or "").strip()
    if not query:
        return []
    try:
        async with httpx.AsyncClient(timeout=30) as client:
            qv = (await embed_texts([query], client))[0]   # 用户的问题 → 1024维向量
        if not qv:
            return []
        results = get_vector_store().query(qv, top_k or config.RAG_TOP_K)  # 余弦top-3
    except Exception as e:
        print(f"[RAG] 检索失败，本条消息降级为无引用：{e}")
        return []                                          # 检索失败≠对话失败
    for i, item in enumerate(results):
        item["index"] = i + 1                              # 编号1起，拼prompt用
    return results
```

> 白话：把用户这句话也送去同一个 embedding 模型变向量，去库里取最像的 3 块。**任何一步出错都返回空列表**——大不了这条回答没有引用卡片，绝不能让"查资料"这个附加动作把"聊天"这个主功能搞挂。这是整条链路最重要的容灾思想。

**第③步：Prompt 组装**（rag.py，逐字迁移自原前端代码——迁移前后模型输入完全一致，这是行为基准）：

```python
RAG_CHAT_SYSTEM_PROMPT = "\n".join([
    "你是Junnnneal AI助手，一位温暖、专业的心理健康陪伴助手。",
    "请先用温和共情的语气回应对方的情绪，再给出具体、可操作的建议；回复保持简洁，一般不超过300字。",
    "如果用户表现出明显的自伤或危机倾向，请优先建议拨打心理援助热线（如希望24热线400-161-9995）或联系信任的人。",
])

def build_rag_context(citations: list[dict]) -> str:
    if not citations:
        return ""
    return "\n\n".join([
        "以下是知识库中与用户问题相关的资料，回答时自然地参考；与问题无关的请忽略，不要在回答里标注来源序号（如[1][2]）：",
        *[f"[{i+1}] 《{c['articleTitle']}》—— {c['heading']}\n{c['text'][:300]}"
          for i, c in enumerate(citations)],
    ])
```

> 白话：system prompt = 人设（第一段）+ 捞出来的资料（第二段，每块截前 300 字、编号成 [1][2][3]）。注意一个设计决策：**编号是给模型看的"地址签"，但要求模型回答里不要报这些序号**——因为来源信息已经作为 citations 事件单独推给前端做成引用卡片了，比让模型在正文里写"[1]"更可靠（模型偶尔会标错，结构化数据不会）。

**第④步 + 整条在线链路的入口**（main.py 的 /rag/chat，节选）：

```python
@app.post("/rag/chat")
async def rag_chat(request: Request):
    body = await request.json()
    message = str(body.get("message") or "").strip()
    history = body.get("history") or []

    async def event_stream():
        try:
            # 知识库就绪等待3s软超时：冷启动撞上灌库时降级为无引用回答，绝不拖首字
            try:
                await asyncio.wait_for(knowledge_base.ensure_built(), timeout=3)
            except Exception:
                print("[RAG] 知识库3秒内未就绪，本条消息降级为无引用对话")

            citations = await rag.retrieve(message)          # ①②检索
            if citations:
                yield _sse({"type": "citations", "citations": [...]})  # 引用卡片先走

            system_content = "\n\n".join(
                x for x in [rag.RAG_CHAT_SYSTEM_PROMPT, rag.build_rag_context(citations)] if x
            )
            msgs = [SystemMessage(content=system_content), *_history_messages(history, message)]

            async for chunk in rag_llm.astream(msgs):        # ④流式生成
                delta = _content_text(chunk.content)
                if delta:
                    yield _sse({"type": "token", "text": delta})
            yield _sse({"type": "done"})
        except Exception as e:
            yield _sse({"type": "error", "message": f"RAG 对话出错：{e}"})

    return StreamingResponse(event_stream(), media_type="text/event-stream", ...)
```

> 白话：一次请求里依次发生——等知识库就绪（最多等 3 秒）→ 检索 → 推引用卡片 → 拼提示词 → 流式生成。SSE 事件顺序固定为 `citations → token...n → done/error`。

### 4.3 工程亮点（面试的"加分项"，讲完流程讲这些）

| 设计 | 解决什么问题 | 一句话讲法 |
|---|---|---|
| **语料指纹增量重建** | 重启后要不要重新花钱 embedding？ | 对全部语料文件算 sha256 存进库里，启动时指纹一致且库非空就直接复用 |
| **单飞 + shield** | 并发请求同时触发建库怎么办？ | 多个请求共享同一个构建任务（不重复建）；某个请求等不及 3s 超时走了，也不能把正在进行的构建连累取消（asyncio.shield） |
| **3s 软超时降级** | 冷启动撞上建库，用户干等？ | 最多等 3 秒，等不到就先正常回答（只是这条没有引用），首字延迟优先 |
| **双向量库后端** | chromadb 在免费层 512MB 内有不确定性？ | 装不上/初始化失败自动降级 JsonStore（纯 Python 余弦暴力检索），122 块规模毫秒级，业务代码零改动 |
| **citations 首事件** | 引用信息等回答结束才知道？ | 检索一完成立刻推引用卡片数据，回答还没开始来源已上屏 |
| **密钥不出服务端** | API key 安全 | GLM key 只存在于 Render 环境变量，前端 bundle 里一个密钥都没有 |

---

## 五、和标准 RAG 架构图逐框对照（就是那张"离线流程/在线流程"图）

| 架构图上的框 | 我项目里的实现 | 代码位置 |
|---|---|---|
| 本地知识文件加载和读取 | 30 篇种子文章 JSON，启动时 lifespan 后台加载 | knowledge_base.py |
| 文本切分 | `<h3>` 前瞻正则切块 + 剥标签 + <20 字丢弃，30 篇→122 块 | knowledge_base.py `_chunk_article` |
| 向量化（Embedding） | GLM embedding-2，10 块/批，按 index 还原顺序 | rag.py `embed_texts` |
| 向量数据库入库索引 | Chroma PersistentClient + cosine 空间，1024 维 | vector_store.py `ChromaStore` |
| Query 向量化 | 用户提问调同一个 embedding 模型 | rag.py `retrieve` |
| 向量匹配 | 余弦 top-3（RAG_TOP_K=3），score = 1−distance | vector_store.py `query` |
| Prompt 工程 | 人设提示词 + 资料组装（逐字迁移自前端） | rag.py `build_rag_context` |
| 提交 LLM 生成答案 | rag_llm.astream 流式 → SSE token → 前端打字机 | main.py `/rag/chat` |

**与图的一处差异（主动讲，变防御为加分）**：图里的离线流程是"构建阶段单独跑一次的批处理"；我把它做成**服务启动自动执行 + 指纹增量**。原因是 Render 免费层磁盘易失（重启数据就丢），离线管线必须能无人值守地自动恢复——冷启动后按指纹判断是否重建，分钟级恢复，不需要人工跑脚本。

**与图示例的选型差异（防追问）**：知识源图上写 Word/PDF/Markdown，我项目是 JSON（语料来自课程后端文章库，已是结构化数据，省掉格式解析层）；向量库图上举 FAISS/Pinecone，我用 ChromaDB（嵌入式零运维）；embedding 图上举 BERT/GloVe，我用 GLM embedding-2 API（中文效果好、免本地算力）；LLM 图上举 GPT-4/文心/通义，我用 GLM-4-flash（免费额度、首字快）。**架构一致，选型按约束（成本、内存、运维）决策**——这句话本身就是面试的好答案。

---

## 六、高频面试 Q&A（12 问）

**Q1：什么是 RAG？一句话。**
检索增强生成。让大模型回答前先从知识库里检索相关资料，拼进提示词再生成。相当于开卷考试，解决模型不知道私有数据、知识过时、幻觉三大问题。

**Q2：RAG 和微调怎么选？**
- 资料频繁更新、要出处、要快上线 → RAG（改资料只需重建向量库，分钟级）
- 要改变模型的能力/风格（如特定语气、领域推理）→ 微调
- 不冲突，可组合：微调管"怎么答"，RAG 管"答的依据"。
我项目文章库会更新且咨询必须可溯源，所以 RAG。

**Q3：为什么用向量检索，不用关键词搜索（如 ES）？**
关键词匹配的是字面：搜"晚上睡不着"匹配不到标题含"失眠"的文章。向量化后比的是语义，"睡不着"和"失眠"坐标天然相近。进阶答法：生产里常做**混合检索**（关键词+向量）+ rerank，我语料小（122 块），纯向量检索已精准，没必要过度设计。

**Q4：chunk 切多大合适？**
没有标准答案，原则是"一块承载一个完整语义单元"。太大：检索不精准、提示词臃肿；太碎：上下文丢失。我按文章的 `<h3>` 小节标题切（天然语义边界，每块自带标题），<20 字碎块直接丢弃，平均一块几百字。

**Q5：检索结果不准怎么优化？**
① 调 chunk 大小和 top-K；② 查询改写（把口语问题先让 LLM 改写成规范查询）；③ 加 rerank 精排（粗召回 20 块→精排取 3 块）；④ 元数据过滤（按分类先缩小范围）；⑤ embedding 文本带"【分类】标题-小节"前缀（我已做，给向量补主题上下文）。

**Q6：什么是幻觉？RAG 怎么缓解？**
模型不知道也一本正经地编。RAG 把真实资料放在模型眼前并要求"照资料回答、无关的忽略"，同时引用卡片让用户能核查出出处。注意是"缓解"不是"根除"——模型仍可能曲解资料，所以高危场景（自伤倾向）我的提示词里强制引导热线，不依赖检索。

**Q7：embedding 模型怎么选？"1024 维"什么意思？**
选型看：语言（中文要好）、维度（信息密度，越高越细腻但越贵越慢）、部署方式（API vs 本地）。1024 维 = 每段文字被表示成 1024 个数字的坐标点，语义近则坐标近。我用智谱 embedding-2：中文效果好、API 免本地算力（部署环境只有 512MB 内存）。

**Q8：top-K 为什么是 3？**
太少（1）单一路经风险高、覆盖不全；太多（>5）提示词稀释重点、成本涨、还引入无关干扰。3 是小语料下的平衡点，且每块截 300 字注入，提示词总长可控。

**Q9：知识库的文档更新了怎么办？**
我做了**语料指纹**：全部语料文件的 sha256 存在向量库元数据里，启动时比对——一致且库非空直接复用（不重复花 embedding 钱），不一致自动清库重建；另有管理端点 `/kb/rebuild`（令牌保护）手动强制重建。

**Q10：RAG 效果怎么评估？**
两层：检索层看命中率/召回率（典型问题是否捞回正确文章）；生成层看忠实度（回答是否真基于检索内容）与相关性。我的现状：人工抽检典型 query（失眠/压力/父母沟通等）命中良好，自动化评测集是明确的待优化项——这样答诚实且显示知道方向。

**Q11：你上线踩过什么坑？**
挑两个讲：① Chroma 的 `collection.modify()` 不允许携带距离函数参数，哪怕值没变也报" Changing the distance function"，只传指纹即可——距离函数创建集合时已固化；② Render 免费层磁盘易失 + 15 分钟休眠：用 UptimeRobot 定时 ping /health 保活，用指纹自动重建对抗磁盘丢失。都有真实报错和修复过程，可信度高。

**Q12：整条链路里前端干了什么？**（前端岗位必被问）
POST 到 `/agent/rag/chat`，原生 fetch 读 SSE 流（buffer 半行拼接解析），先收 citations 渲染引用卡片，再逐 token 喂给 rAF 打字机（积压自适应速度），error 事件走统一错误处理。**检索、向量、密钥全在服务端**，前端零 AI 逻辑——这也是从浏览器端 RAG 迁到服务端的原因（密钥安全、成本可控、Agent 页复用）。

---

## 七、面试前 10 分钟速记卡

**必背数字**：

| 项 | 值 |
|---|---|
| 知识库规模 | 30 篇文章 → 122 块 |
| embedding | GLM embedding-2，1024 维，10 块/批 |
| 检索 | ChromaDB 余弦 top-3，score = 1 − distance |
| 实测命中 | "失眠" → CBT-I 文章，最高 score ≈ 0.64 |
| 建库耗时 | 约 4.8s（30 篇全量） |
| 软超时 | 3s（库没就绪先回答，无引用） |
| 首字延迟 | 190~240ms（固定版本 glm-4-flash-250414） |
| 部署 | Render（免费层）+ Netlify 边缘函数转发 |
| 保活 | UptimeRobot 定时 ping /health（免费 750h/月 > 全月 744h） |

**必背流程**（各 4 步）：
- 离线：加载 → 切块 → 向量化 → 入库索引
- 在线：Query 向量化 → 向量匹配 top-K → Prompt 组装 → LLM 生成

**必背三个"为什么"**：
- 为什么 RAG 不微调 → 资料常更新、要出处、低成本
- 为什么向量检索不关键词 → 匹配语义不匹配字面
- 为什么 ChromaDB → 嵌入式零运维、自带落盘、小规模无性能差别

**必背一个工程故事**（最能体现深度）：Render 磁盘易失 → 离线管线不能"跑一次就完"→ 挂在启动生命周期里 + sha256 指纹增量重建 + 单飞任务防并发重复建 + 3s 软超时保首字延迟。
