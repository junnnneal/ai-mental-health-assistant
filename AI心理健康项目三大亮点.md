# AI 心理健康项目三大亮点（面试总结版）

> 配套文档：《AI心理健康情绪陪伴助手项目梳理.md》（全项目细节）、《RAG零基础面试讲解.md》（RAG 从零讲起）、《AI心理健康项目RAG升级代码对照.md》（迁移全过程代码对照）。
> 用法：每个亮点按「简历表达 → 背景难点 → 我做了什么 → 结果 → 可能被追问」组织。先背**简历表达**和**结果**（面试最先被问到的两样），再理解中间的过程。

---

## 亮点一：SSE 流式对话全链路

### 简历表达（可直接进简历）

> 实现 AI 对话的 SSE 流式全链路：服务端边生成边推送（citations → token → done 事件契约），前端原生 fetch 读流解析 + rAF 打字机自适应渲染；通过分段埋点定位瓶颈，首字延迟从最差 1.8s 优化至 190~240ms。

### 背景与难点

- 课程后端 `/psychological-chat/stream` 返回 HTTP 200 后挂死不出数据（用户看到"一直繁忙"的根因），且后端没有追加消息接口 → 弃用该通道，改前端直连 GLM，RAG 上服务端后统一走自建 agent-server。
- 流式渲染三座大山：**SSE 分片的解析边界**、**逐字渲染的性能**、**Vue 响应式陷阱**。

### 我做了什么

1. **手写 SSE 解析器**（`src/apis/agent.ts`）：原生 fetch 读流 + buffer 半行拼接，处理 chunk 在 `data:` 行中间断开的边界情况；不用 fetchEventSource 库，因为需要 POST + 自定义请求头。
2. **rAF 打字机**（`Consultation.vue`）：requestAnimationFrame 逐字渲染，按 token 积压量自适应速度（token 到得快就打快，不积压）；surrogate pair 处理保证 emoji 不乱码。
3. **响应式陷阱踩了两次，第二次秒定位**：assistant 消息必须 `reactive()` 包装再 push 进数组，否则 SSE 回调改的是原始对象、不触发重渲染 → 整段答案在流结束时一次性出现（"假流式"）。咨询页和健康管家页各踩一次，第二次靠第一次的注释直接定位。
4. **首字优化靠数据不靠猜**：分段埋点定位出两处瓶颈——
   - TEMP 会话先 `await startSession()` 阻塞数秒 → 改并行：立即开流，建会话后台单飞，成功后迁移消息桶；
   - 模型别名 `glm-4-flash` TTFT 抖动 0.4~1.8s → 换固定版本 `glm-4-flash-250414`，稳定 190~240ms（约 4 倍提升）。
5. **事件契约设计**：`citations` 首事件（检索无命中整帧省略）→ `token` → `done`/`error`；错误以事件形式下发，绝不裸断流。
6. **瞬态网关错误自动重试**（`src/apis/agent.ts`）：Render 免费层冷启动/OOM 重启的窗口期，Netlify 边缘函数会回 502/504（或网络层直接失败）——前端只对 502/503/504 在"响应尚未开始"时等 3s 自动重试一次；流已开始就不重发（防回复内容重复），非瞬态 4xx 不重试直接走调用方错误处理；错误信息带响应体片段，标明是哪一跳挂的。

### 结果

- 首字延迟稳定 190~240ms（优化前最差 1.8s+）。
- vite 代理层开销实测 ≈ 0（p50 411 vs 421ms）。
- 打字机渲染全程流畅，无长任务阻塞。

### 可能被追问

- **为什么用 SSE 不用 WebSocket？** → 单向推送场景，SSE 基于纯 HTTP、免协议升级，穿代理/CDN/边缘函数兼容性最好（我们的链路要穿透 Netlify 函数透传）；需要双向低延迟才上 WebSocket。
- **打字机为什么用 rAF 不用 setInterval？** → 与浏览器渲染节拍对齐、切后台自动暂停；interval 回调与渲染错拍会掉帧。
- **怎么确认是真流式不是假流式？** → 服务端 curl 实测 token 渐进到达（约 15ms/个）；"假流式"的根因是响应式包装缺失，不是网络。
- **为什么 502/504 等三秒重试一次就有用？** → 瞬态故障的本质是免费实例的重启窗口（几十秒），等 3s 实例缓过来基本就通；重试的安全性靠"流未开始才重试"保证——SSE 已经吐过 token 再重发会导致回复重复，那种情况宁可报错。

---

## 亮点二：IndexedDB 替代 localStorage 做会话持久化

### 简历表达

> 设计聊天记录浏览器本地持久化方案：IndexedDB 桶模型（userId_sessionKey 索引 + seq 自增保序）+ localStorage 存量幂等迁移 + 单桶容量裁剪 + 隐私模式降级；定位并修复 reactive Proxy 不可结构化克隆导致的静默落库失败。

### 背景与难点

- 原方案 localStorage 的三宗罪：**5MB 上限**、**同步阻塞主线程**、**只能存字符串**（消息 + 引用卡片是嵌套结构，存取都要手动序列化）。
- 关键约束：AI 消息只存在浏览器本地（课程后端无追加消息接口，GLM 侧不留存）→ **本地存储就是聊天记录的唯一真相源**，可靠性要求高。

### 我做了什么

1. **桶模型**：以 `userId_sessionKey` 为桶（object store 的键），桶内 seq 自增保证消息顺序；删会话即删桶。
2. **容量治理**：单桶 200 条裁剪，防单会话无限膨胀。
3. **存量迁移**：v1 localStorage 数据幂等迁移（迁完打标，重启不重迁）。
4. **TEMP 会话转正**：首次发消息立即开流不等 `session/start`（建会话后台单飞防重复），成功后迁移整个消息桶，失败不拦 AI 回答。
5. **最硬的一个 bug**：reactive 消息浅拷贝后嵌套 citations 仍是 Proxy，`structuredClone` 抛 DataCloneError → **所有带引用卡片的 AI 回复静默落库失败**（用户报"新对话不留历史"的真凶）。修复 = 入库前 `JSON.parse(JSON.stringify(msg))` 剥代理。
6. **降级**：无痕模式 IndexedDB 不可用时静默降级内存模式，主流程不受影响。

### 结果

- 聊天记录、引用卡片、多会话隔离全部持久化，清页刷新不丢。
- 异步 API 不阻塞主线程；直接存结构化对象，免手动序列化。

### 可能被追问

- **为什么不上服务端存储？** → 课程后端无追加消息接口；改造成本与收益不匹配 + 心理倾诉类内容存本地对用户更隐私友好。这是与用户对齐后的产品决策，且方案保留了升级路径。
- **localStorage 与 IndexedDB 本质区别？** → 同步 vs 异步、约 5MB vs 数百 MB、纯字符串 vs 结构化对象；两者都按 Origin 隔离。
- **DataCloneError 的根因？** → Vue 的 reactive 是 Proxy 包装，structuredClone 只接受可克隆类型，Proxy 不在其中；深拷贝一次把代理剥掉即可。

---

## 亮点三：外接 LLM + 服务端 RAG（AI 咨询与情绪分析）

### 简历表达

> 基于智谱 GLM 构建服务端 RAG 咨询链路（FastAPI + ChromaDB + embedding-2：150 篇心理文章 → 653 块 → 向量粗排 top-10 + bge-reranker 精排 top-3 + 双尺阈值过滤，指代型追问自动拼上下文重检 → prompt 注入 → 流式生成 + 引用卡片），并用 LangGraph 搭建 ReAct Agent（知识检索/情绪分析/会话查询/日记保存四工具）；Netlify + Render 双平台部署，密钥全链路不出服务端。

### 背景与难点（三阶段演进，面试主动讲）

1. **阶段一**：课程后端 SSE 挂死 → 弃用，前端直连 GLM（治"不能用"）。
2. **阶段二**：浏览器端 RAG（治"回答没有依据"）→ 上线受限：embedding 调用成本不可控、API key 必须进前端、健康管家 Agent 页用不上。
3. **阶段三**：RAG 整体迁服务端并部署上线（治"上不了线"）——最终形态。

### 我做了什么

1. **迁移纪律**：system prompt 与 context 组装逻辑从 Vue 组件**逐字迁移**到 `rag.py`，保证迁移前后模型输入完全一致——这是行为基准，不是趁机重写。
2. **离线管线**：`<h3>` 前瞻正则按小节分块（<20 字碎块丢弃；向量化文本带"【分类】标题 - 小节"前缀提升命中率）→ embedding-2 批量向量化（10 条/批，按 index 还原顺序）→ Chroma cosine 入库。
3. **并发安全的构建**：sha256 语料指纹增量重建（内容没变就跳过）；单飞 Task + `asyncio.shield`（等待方 3s 软超时取消不连带杀掉进行中的构建）+ `_build_lock` 串行化防交叉写库。
4. **可靠性设计**：知识库未就绪 3s 软超时降级为无引用回答，绝不拖首字；chromadb 不可用自动降级 JsonStore（纯 Python 余弦）双后端保险丝；检索任何失败返回空列表，绝不拖垮对话主链路；rerank 超时/失败回退向量原序——精排是增强项，挂了只损失排序质量，不新增故障面。
5. **体验设计**：`citations` 作为 SSE 首事件——回答还没开始，引用卡片先渲染；模型回答本身不标 [1][2]，来源统一由卡片展示。
6. **部署**：Render 免费层（512MB 内存 / 15 分钟休眠 → UptimeRobot + GitHub Actions 定时 ping `/health` 双保活——Actions 每 13 分钟一次 curl，非 200 借失败通知当免费宕机告警，私有仓库免费额度 2000 分钟/月够用；ephemeral 磁盘重启丢库 → 指纹校验自动重建）+ Netlify 边缘函数代理（AGENT_URL 环境变量），GLM key 只存在于 Render 环境变量。
7. **语料扩容运营**：30 → 150 篇（10 类主题、653 块）——按实测召回失败的 query 定向补缺（"提加薪"只召回 0.41 弱卡，根因是语料缺口而非检索算法）；11 批并行起草 + 每主题联网检索循证素材 + 统一格式规范，`verify-seed-corpus.mjs` 校验（JSON 合法/全局标题查重/h3 结构/块数统计，可重跑）；`import-articles.mjs` 改 glob 读取，新增批次零改动接入。
8. **检索质量三件套（逐项实测落地）**：
   - **两阶段检索**：向量粗排 top-10 → SiliconFlow 托管 bge-reranker-v2-m3 精排取 top-3。512MB 实例跑不动 cross-encoder（权重 fp16 就 1.1GB，0.1 核 CPU 单对前向秒级），托管 API 一条 HTTP 换别人的 GPU；模块级长连接复用（冷启 1.4s → 稳态 180ms）；未配 key 自动退化为纯向量，配置即开关、无开关变量。
   - **双尺阈值过滤**：rerank 尺 0.05 / 余弦尺 0.40 分开配不混用——实测 rerank 区分度比余弦高两个数量级（相关 ≥0.1、噪声 ≤0.005），精排在场用 rerank 尺，关闭时余弦尺兜底；弱卡不进 prompt 不出卡片，被滤分数打进日志持续免费校准。
   - **指代型追问重检**："第二种方法是什么"会模糊命中无关块（实测 0.31，非空但错，所以触发线不能只看空结果）——空结果或余弦最高分 <0.45 才拼最近一条用户提问重检、分更高才替换；独立问题首轮高分通过，行为不变；不做永远拼接（历史稀释当前问题向量）。

### 结果

- 语料按实测召回失败定向扩容 30 → 150 篇（122 → 653 块，10 类主题）；30 篇时建库约 4.8s，653 块实测本地重建约 10s（后台进行，3s 软超时照旧不拦首字）。检索实测精准（"失眠" → CBT-I 文章，相似度 0.64）。
- 检索质量三件套实测：① "提加薪" 0.41 弱卡 → 0.68 三连中（语料扩容 + 精排合力）；② rerank 分数带 0.0000~0.84，比余弦（0.26~0.67）高两个数量级的区分度；③ 噪声 query（"1+1等于几"）top-3 全滤、零引用零知识库污染；④ 指代追问"第二种方法是什么"从错卡 0.31 → 对卡 0.61（top1 恰好是上一轮提过的"规则二：睡不着就离开床"）。
- 4 个端点 curl 验证 + 线上浏览器全链路通过。
- 首字延迟不因加检索而劣化（3s 软超时兜底）。

### 可能被追问

- **为什么迁服务端？** → 密钥安全（key 不进前端 bundle）、embedding 成本可控、健康管家 Agent 复用同一知识库、浏览器端网络/CORS 受限。
- **为什么选 ChromaDB？** → 嵌入式零运维（pip 装完即用，不需要独立数据库服务）、PersistentClient 自带落盘、百级数据量下与 FAISS/Pinecone 无性能差别；且设计了 JsonStore 兜底，chromadb 装不上自动降级。
- **RAG 效果怎么评估？** → 检索层（命中率/召回）+ 生成层（忠实度、是否引用了检索内容）；目前靠人工抽检典型 query 的命中情况（阈值与重试触发线的分数带全部来自实测而非拍脑袋），自动化评测是明确的待优化项（诚实说法）。语料规模按查询覆盖率定而不是按篇数定：下一步攒 30~50 条真实 query 评测集跑 top-3 命中率做回归，miss 聚集的主题定向补。检索质量三件套（两阶段精排、双尺阈值过滤、指代型追问重检）已全部落地并逐项实测。

---

## RAG流程（从语料到回答的完整链路）

> 亮点三的展开版，按"分块 → 向量化 → 索引 → 存储 → 检索重排 → 生成"六步拆。一句话串联：**种子文章 → `<h3>` 分块 → embedding-2 向量化 → 嵌入式 Chroma（HNSW/cosine）→ 查询时向量粗排 top-10 + rerank 精排 top-3 + 阈值过滤（弱命中拼上下文重检）→ 拼进 system prompt → GLM 流式生成 + 引用卡片前置下发**。

### 0. 语料来源：自建 150 篇种子文章，同一份语料两个消费者

- **语料本身**：`scripts/seed-articles/` 下 13 个 JSON（articles-1~13），共 150 篇、10 大主题（情绪管理/睡眠健康/压力应对/人际沟通/自我成长/心理科普 + 职场心理/情感关系/家庭关系/学业成长），每篇 `title/category/tags/summary/content` 五字段；**content 是带 `<h3>` 小节的结构化 HTML**——写语料时就把"面向检索切分"设计进去了（后面的分块实现建立在这个结构上）。
- **从 30 到 150 怎么扩的**：不是拍脑袋灌量，是**按实测召回失败的 query 定向补缺**——"如何向领导提加薪"只召回 0.41 的弱卡，根因是职场谈判类语料缺失；新增 4 个类目（职场/情感/家庭/学业）正对着目标用户（学生+年轻职场人）的高频困扰。生产管线：11 个批次并行起草，每主题先联网检索循证素材（CBT/正念/睡眠医学等主流方法）再按统一格式组织；写完跑 `verify-seed-corpus.mjs` 校验——JSON 合法性、全局标题查重、h3 结构、块数统计，脚本可重跑，改语料随手验。
- **规模为什么定在 150**：按查询覆盖率定不按篇数定——目标是典型 query 的 top-3 命中率，miss 聚集在哪个主题就补哪个。150 篇（~653 块）同时卡在工程甜点位：检索进入"需要区分度"的区间（rerank 开始有可测效果）、512MB 内存毫无压力、冷启动重建本地实测约 10s 可控。再往上到千篇级，近似重复块会让粗召回噪声上升（precision 反而掉），需要换语料源 + 自动去重管线，是另一个架构阶段。
- **路径 A（课程后端 → 前台知识库页）**：`import-articles.mjs` 用管理员 token 把 150 篇发布到课程共享后端（自动查重 + 10 个种子类目映射到后端固定的 4 个分类；文件列表 glob 读取，新增批次零改动接入）；前台知识库页从 `/api` 拉展示。保留脚本随时重跑是**灾备**：全班共用一个库会被同学清库/污染（真实发生过 35 篇 → 8 篇垃圾 → 0 知识块）。
- **路径 B（RAG 服务端内置，最终形态）**：`agent-server/data/` 放一模一样的 13 份 JSON，启动直接读文件灌库，**不经过后端**。两个原因（写在 `knowledge_base.py` 文件头）：① 共享后端会被清库，内置源不依赖外部存活；② 后端接口要用户 token，服务启动时没有可用身份。
- **语料 = 指纹**：`data/*.json` 内容的 sha256 就是向量库重建指纹——改文章只需改 JSON 重启，指纹失配自动重建，其他什么都不用动。

### 1. 切片：基于 `<h3>` 小节的语义分块（`knowledge_base.py` 的 `_chunk_article`）

不是固定长度切窗，是**按文章结构切**：

1. **前瞻正则分割**：`re.split(r"(?=<h3>)", html)` 在每个 `<h3>` 标签前下刀（零宽前瞻，标签留在下一段开头，小节不丢标题）；
2. **提小节标题**：段内第一个 `<h3>` 文字作为块的 `heading`，无 h3 的开头段回退用文章标题；
3. **剥 HTML 标签**转纯文本，**<20 字的碎块丢弃**（过滤空小节、导航残渣）；
4. **稳定 ID**：`{文章id}_{小节序号}`，文章 id 又由 `seed:{文件名}:{序号}` 构成——重启/重建不漂移，历史会话里持久化的引用卡片不会指向错文章。

两个关键设计：

- **embed_text ≠ 展示文本**：向量化文本带 `【分类】标题 - 小节\n正文` 前缀——CBT-I 文章的某小节单看正文可能不带"失眠"二字，前缀保证查询"失眠"的语义命中率（实测 0.64 的功臣之一）；
- **为什么 h3 不固定 500 字**：语料是结构化 HTML，小节即天然语义单元，块内主题单一、命中后整块可读；固定窗口会把句子拦腰切断，召回的块语义残缺。代价是依赖语料格式规整——接入无结构文本（markdown/PDF）需加一层标题层级归一化。

### 2. 文本 → 向量：智谱 embedding-2，1024 维（`rag.py` 的 `embed_texts`）

- **远程 API 非本地模型**：httpx 异步 POST 智谱 embedding 接口，key 只在服务端环境变量；
- **批量**：按批切分（150 篇 ≈ 653 块 ≈ 66 批；语料从 30 篇 13 批扩上来时，批量化让建库时间只随块数线性增长）；
- **响应不保证顺序**：靠返回条目的 `index` 字段回填还原（`ordered[item["index"]] = item["embedding"]`），否则块与向量错位；
- **同源约束（硬前提）**：入库（文档侧）与查询（用户问题，`retrieve` 里同样调 `embed_texts`）必须**同一模型**——查询向量与文档向量同空间，余弦相似度才有意义。

### 3. 索引：Chroma 集合 + HNSW 近似最近邻（`vector_store.py` 的 `ChromaStore`）

- 建集合时固化 `metadata={"hnsw:space": "cosine"}`（距离空间创建后不可改，chroma 1.5+ 的 `modify` 带 hnsw:space 会报错——踩过的坑）；
- `upsert(ids, embeddings, metadatas, documents)` 四元组：**id**（稳定块ID）、**embedding**（1024维）、**metadata**（articleId/articleTitle/heading——引用卡片直接从这出）、**document**（块正文）；
- Chroma 内部自动建 **HNSW**（分层可导航小世界图，多层稀疏图贪心下沉，近似对数复杂度）——不需要手写索引代码；
- 查询返回 cosine distance，代码换算 `score = 1 - dist`，与前端手写余弦的分数语义对齐；
- **诚实讲法**：653 块的规模 HNSW ≈ 精确检索，ANN 的价值要到十万级以上才显现——所以留了 JsonStore 纯 Python 暴力余弦后端（毫秒级跑完、质量零差别），"我知道 ANN 在这个规模是杀鸡用牛刀"这个认知本身就是加分项。

### 4. 向量库在哪：嵌在应用进程里，不是独立服务

- **物理位置**：`PersistentClient(path=chroma_data/)`，本地是 SQLite + 索引文件目录，线上在 Render 容器磁盘——**嵌入式模式，库随应用走**，不是独立部署的 Chroma server / Qdrant；
- **ephemeral 磁盘兜底**：Render 免费版重启丢盘 → 启动时 lifespan 校验语料 sha256 指纹，发现空库/失配自动重新分块+向量化灌库（653 块本地实测约 10s）——"库丢了"≠"服务挂了"。

**为什么选嵌入式（架构取舍）：**

1. **规模不需要**：653 块几 MB 索引，独立向量库服务意味着多一个常驻进程、一套端口鉴权、一跳网络——纯负资产；
2. **部署约束锁死选项**：Render 免费版 = 一个服务 + 512MB；独立向量库要第二个实例，同样 15 分钟休眠、同样 512MB，"向量库冷启动"与"应用冷启动"叠加，可用性反而更差；嵌入式让整条链路只有一个要保活的进程；
3. **性能与可靠性更好**：进程内是函数调用 vs 网络往返，省 1~5ms 且少一个可能超时的下游——对"检索失败必须静默降级不拖垮对话"的原则，少一跳少一个故障点；
4. **运维简单**：无应用↔数据库连接管理/认证/健康检查，单人项目每少一个组件少一类报警。

**代价（主动说出来）：** 多实例无法并发读写嵌入式 Chroma（无多写者协调），水平扩展时须切 Chroma server / Qdrant——好在业务层只依赖 `VectorStore` Protocol 接口，切换是换一个类的事；索引与应用同进程抢 512MB（OOM 风险背景，也是 `KB_BACKEND=json` 保险丝存在的原因）；备份/生命周期与应用盘绑定。

### 5. 检索与重排：向量粗排 + rerank 精排 + 双阈值 + 指代重试（查询时刻，`rag.py` 的 `retrieve` + `main.py` 的 `/rag/chat`）

- **完整链路**：问题向量化 → 向量粗排 top-10 → rerank 精排重排序 → 取 top-3 → 阈值过滤（弱卡出局）→ 进 prompt；首轮弱命中或空结果，再拼最近一条用户提问重检一次。
- **为什么要两阶段**：向量检索是"双塔"结构（query 和文档各自独立编码，再算距离），快但粗——实测余弦分数带挤在 0.26~0.67，好块坏块拉不开；cross-encoder 把 query 和文档拼成一对过完整 transformer，逐对交叉注意力，精度高但每对一次前向、注定慢。工程上取长补短：向量先便宜地捞 10 个（`RERANK_CANDIDATES=10`），cross-encoder 只精挑这 10 个。
- **精排模型为什么跑在别人的机器上**：bge-reranker-v2-m3 权重 fp16 约 1.1GB，Render 512MB 总内存装不下；0.1 核 CPU 单对前向秒级、10 对 10~30s 直接不可用——所以调 SiliconFlow 托管 rerank API，一条 HTTP 换别人的 GPU 常驻。工程细节：模块级 `httpx.AsyncClient` 长连接复用（冷启 TLS 1.4s → 稳态 180ms）；2s 超时回退向量原序；未配 key 静默退化为纯向量，配置即开关、没有开关变量。
- **双尺阈值（易踩坑，主动讲）**：rerank 分数与余弦分数不是一个尺度、分布也不同——实测 rerank 相关 ≥0.1 / 噪声 ≤0.005（区分度两个数量级），余弦命中 0.58+ / 错得再像也只有 0.31。所以 `RERANK_MIN_SCORE=0.05` 与 `RAG_MIN_SCORE=0.40` 两把尺子分开配、谁在场用谁；弱卡不进 prompt 不出卡片，被滤分数打进日志（噪声 query"1+1等于几"实测滤掉 [0.005, 0.003, 0.003]，零卡片零污染）——阈值校准成了持续免费的副产品。引用卡片对外展示的分数恒为余弦分，两把尺子绝不混上 UI。
- **指代型追问重检**："第二种方法是什么"单看当前消息，会模糊命中"第二步怎么做"这类序数语义的无关块（0.31，非空但错——所以触发线不能只看空结果）——空结果或余弦最高分 <0.45（`RAG_RETRY_BELOW`）才拼最近一条用户提问重检、重检分更高才替换。只在弱命中时花这次额外检索：独立完整问题首轮高分直通、行为不变；不做永远拼接——历史话题会稀释当前问题向量，独立问题反而被带偏。实测：失眠话题下追问，错卡 0.31 → 对卡 0.61（top1 正是上一轮提过的"规则二：睡不着就离开床"）。

### 6. 生成：prompt 组装 + 流式输出（`main.py` 的 `/rag/chat` + `rag.py` 的 `build_rag_context`）

- **citations 首事件先行**：检索一完成、回答还没开始，引用卡片数据就作为 SSE 首事件下发（无命中整帧省略）——用户先看到"来源"再看"回答"，检索的存在感前置；事件契约 `citations → token → done/error`，错误以事件下发、绝不裸断流。
- **system prompt 两段拼接**：人设（温暖共情 + 可操作建议 + 自伤危机热线兜底）+ 资料上下文。人设是从原 Consultation.vue **逐字迁移**的——迁移纪律：迁移前后模型输入完全一致，这是行为基准，不是趁机重写。
- **资料注入格式**：一句指令 + `[序号] 《文章标题》—— 小节标题 + 正文截 300 字`。top-3 × 300 字 ≈ 1千字资料，prompt 长度恒定可控；截断只影响资料段，人设不动。
- **指令设计挡两类坑**："与问题无关的请忽略"——检索层阈值过滤挡了第一道，生成层指令再挡一道，防模型把弱相关资料硬编进回答；"不要标注来源序号[1][2]"——来源展示是前端引用卡片的职责，模型再标序号就与卡片重复打架（检索与展示的 UX 分工）。
- **多轮上下文与指代分工**：最近 10 条历史 + 当前消息（与 /chat 同规则）。指代理解在生成层天然解决——第 5 步的重检只负责"查对资料"，"第二种方法指什么"由对话历史让模型自己消解，两层各管一段。
- **流式生成**：`rag_llm.astream` 逐 chunk 转 `token` 事件推给前端打字机渲染；检索全部发生在流开始之前，所以加 RAG 不拖首字（知识库 3s 软超时兜底）。

### 面试串联话术（30 秒版）

> "RAG 全链路：150 篇结构化文章按 `<h3>` 小节语义分块、拼'分类+标题+小节'前缀后用 embedding-2 批量向量化成 1024 维向量，upsert 进嵌入式 ChromaDB 的 cosine 集合（HNSW 索引自动建）。用户提问时把问题也向量化、向量粗排 top-10，再用托管 bge-reranker 精排取 top-3、双尺阈值过滤掉弱卡，指代型追问弱命中时自动拼上一条提问重检一次；然后引用卡片作为 SSE 首事件先于回答渲染，资料截 300 字拼进 system prompt、最近 10 条历史带上，GLM 流式生成，模型不标序号、来源交给卡片。检索链四层兜底：知识库没就绪 3 秒软超时降级、检索异常返回空列表、chromadb 不可用切 JsonStore 暴力余弦、rerank 挂了回退向量原序——任何情况下 RAG 故障都不拦着 AI 回话。"
